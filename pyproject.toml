[build-system]
requires = [
    "setuptools >= 48",
    "wheel >= 0.37.1",
    "twine >= 4.0.0",
]
build-backend = 'setuptools.build_meta'

[tool.semantic_release]
version_variable = "setup.py:__version__"